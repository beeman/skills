interface:
  default_prompt: "Use $feature-structure to propose a composition-first feature structure with parent gating, split data-access hooks, thin routes, and granular UI leaves."
  display_name: "Feature Structure"
  short_description: "Refactor or create real composition-first features"
