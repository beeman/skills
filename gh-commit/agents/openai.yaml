interface:
  default_prompt: "Use $gh-commit to get onto the correct feature branch if needed, inspect the tracked diff, prepare or amend a clean commit with any required changeset, and stop before pushing or opening a PR."
  display_name: "Commit Changes"
  short_description: "Prepare a clean local Git commit"
